language: generic 

sudo: required
services: 
  - docker

before_install:
  - docker build -t nicolasfmh/docker-react - f Dockerfile.dev .
script:
  - docker run nicolasfmh/docker-react npm run test -- --coverage